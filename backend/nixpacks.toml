[phases.setup]
nixPkgs = ["python312", "postgresql_16.dev", "gcc", "g++"]

[phases.install]
dependsOn = ["setup"]
cmds = [
  "pip install --upgrade pip",
  "pip install poetry",
  "poetry install --no-root"
]

[start]
cmd = "poetry run uvicorn app.main:app --host 0.0.0.0 --port $PORT"